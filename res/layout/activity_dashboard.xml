<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <com.handmark.pulltorefresh.library.PullToRefreshGridView
        xmlns:ptr="http://schemas.android.com/apk/res-auto"
        android:id="@+id/dashboardUsersGrid"
        android:layout_height="fill_parent"
        android:layout_width="fill_parent"
        android:numColumns="auto_fit"
        android:verticalSpacing="10dp"
        android:horizontalSpacing="10dp"
        android:columnWidth="90dp"
        android:stretchMode="columnWidth"
        android:gravity="center"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        ptr:ptrMode="both"
        ptr:ptrDrawable="@drawable/android" />

    <RelativeLayout
        android:id="@+id/dashboardBottomLayout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#cccccc"
        android:visibility="invisible"
        android:paddingBottom="@dimen/dashboard_footer_top_margin"
        android:paddingLeft="@dimen/dashboard_footer_left_padding"
        android:paddingRight="@dimen/dashboard_footer_left_padding"
        android:paddingTop="@dimen/dashboard_footer_top_margin" >

        <Button
            android:id="@+id/dashboardNearByUsersBtn"
            style="@style/button_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:background="@drawable/orange_button"
            android:text="@string/dashboard_near_users_btn" />

    </RelativeLayout>
 
	<com.supermanket.utilities.SideNavigationView
        android:id="@+id/side_navigation_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
         />
 
</RelativeLayout>